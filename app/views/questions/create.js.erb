$('.question-errors').html(`<%= render 'shared/errors', resource: question %>`)
<%if question.persisted?%>
  $('.questions').prepend('<%= j render question %>')
  $('#question_body').val('')
  $('#question_title').val('')
  $('.question-form').hide()
  $('.question-link').show()
<%end%>
