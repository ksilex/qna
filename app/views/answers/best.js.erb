// $('.answer').first().removeClass('best-answer')
// $('.fa-check').remove()
// $('[data-answer-id="<%#= answer.id %>"]').remove()
// $('.answers').prepend('<%#= j render answer %>')
// то что сверху работает, но если отметить все ответы, то ни на одном не будет ссылки на best_answer до f5
$('.answers').html('<%= j render @answers %>')
